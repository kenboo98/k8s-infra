apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: test-site-route
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`test.kenta.ca`)
      kind: Rule
      services:
        - name: personal-app-service
          port: 80